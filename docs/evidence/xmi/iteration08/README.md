# Iteration 08 — Real Enterprise Architect acceptance checklist

Automated evidence is produced locally with `npm run proof:xmi` under ignored `backend/generated-test-output/xmi-proof/`. It proves only the `crunch_uml`-based internal semantic round-trip.

Real Sparx Enterprise Architect was not found in the execution environment, so no screenshots, EA version, or successful EA result are claimed.

Real EA execution is intentionally deferred, not forgotten. The authoritative checklist and closure conditions are tracked as [`EA-XMI-001`](../../../quality/PENDING_ACCEPTANCE_GATES.md).

Current result for both directions: **BLOCKED — Enterprise Architect unavailable**.
